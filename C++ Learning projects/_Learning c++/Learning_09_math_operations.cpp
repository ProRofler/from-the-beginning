#include <iostream>
#include <cmath>



int main() {

double x = 4.87;
double y = 2.3;
double z;

//z = std::max (x, y); - returns the max
//z = std::min (x, y); - returns the min
//z = pow (x, y); - powers the one by another
//z = sqrt(16); - square root
//z = abs (-5); - abslolute, no negative number
//z = round (y); - rounds the number
//z = ceil (y); - tops the double number
//z = floor (x); - floors the double number






std::cout << z;
    return 0;
}